Sogeti internal OOS project DeliveryBoy

Global setup:

 Download and install Git
  git config --global user.name "Boris Prochazka"
  git config --global user.email bopr@sogetistockholm.se
        

Next steps:

  mkdir Deliveries
  cd Deliveries
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:borisprochazka/Deliveries.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:borisprochazka/Deliveries.git
  git push -u origin master


Clone your own Repo

  git clone git@github.com:borisprochazka/Deliveries.git
  ... edit, add, remove ...
  git commit
  git push
